import { useState } from "react";

export default function BahriiiPage() {
  const [step, setStep] = useState(0);

  return (
    <div className="min-h-screen bg-gradient-to-b from-green-100 to-green-200 flex items-center justify-center p-6">

      <div className="max-w-md w-full bg-white rounded-3xl shadow-2xl border border-green-200 overflow-hidden">

        <div className="p-8 text-center text-green-900">

          {/* STEP 0 */}
          <div
            className={`transition-all duration-700 ease-out
            ${step === 0
              ? "opacity-100 translate-y-0 blur-0"
              : "opacity-0 -translate-y-4 blur-sm pointer-events-none absolute"}`}
          >
            <div className="bg-green-100 rounded-2xl p-6 mb-6">
              <p className="text-lg mb-4">جيبي بوسة</p>

              <div className="flex gap-3 justify-center">
                <button
                  onClick={() => setStep(1)}
                  className="bg-green-600 text-white px-4 py-2 rounded-xl active:scale-95 transition"
                >
                  حاضر يبابا
                </button>

                <button
                  onClick={() => setStep(1)}
                  className="bg-green-700 text-white px-4 py-2 rounded-xl active:scale-95 transition"
                >
                  حاضر بابا
                </button>
              </div>
            </div>
          </div>

          {/* STEP 1 */}
          <div
            className={`transition-all duration-700 ease-out
            ${step === 1
              ? "opacity-100 translate-y-0 blur-0"
              : "opacity-0 translate-y-4 blur-sm pointer-events-none absolute"}`}
          >
            <h1 className="text-4xl font-bold mb-4">Bahriii</h1>

            <p className="text-base leading-relaxed mb-6">
              زوز شطورررة انتي بتقدري ي عسل بحبك
            </p>

            <p className="text-base leading-relaxed mb-4">
              i know work gets stressful sometimes but i’m really proud of u fine shyt
            </p>

            <p className="text-base leading-relaxed mb-6">
              thank u for always trying ur best every single day
            </p>

            <p className="text-xl font-semibold mt-6">— Benbenooo</p>
          </div>

        </div>
      </div>
    </div>
  );
}
